
  # Card Position Animation

  This is a code bundle for Card Position Animation. The original project is available at https://www.figma.com/design/WESFafhpV90RrDPdOzDNyU/Card-Position-Animation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  